import { RightNodeDirective } from './right-node.directive';

describe('RightNodeDirective', () => {
  it('should create an instance', () => {
    const directive = new RightNodeDirective();
    expect(directive).toBeTruthy();
  });
});
